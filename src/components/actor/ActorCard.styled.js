import styled from "styled-components";

import { SerachCard } from "../styled";

export const StyledActorCard = styled(SerachCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;
